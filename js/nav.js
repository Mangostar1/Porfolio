/* window.onscroll = () => {
    let scroll = window.scrollY;
    if(scroll > 500){
        document.getElementById('header').style.cssText = 
        'background-color: rgba(192, 192, 192, 0.50); backdrop-filter: blur(10px); box-shadow: 0px 0px 10px 0px rgb(36, 36, 36, 0.2); position: fixed; top: 0; left: 0; right: 0; z-index: 10;';
    }
    else{
        document.getElementById('header').style.cssText = 'background-color: transparent;';
    }
} */